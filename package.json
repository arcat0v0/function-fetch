{
  "name": "function-fetch",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "build:node": "tsc --outDir dist-node",
    "start:node": "pnpm run build:node && node dist-node/node_server.js"
  },
  "dependencies": {
    "hono": "^4.11.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.9",
    "typescript": "^5.4.5",
    "@types/node": "^25.0.2",
    "wrangler": "^4.4.0"
  },
  "engines": {
    "node": ">=22"
  }
}
